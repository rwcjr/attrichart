## What

Describe the change and why.

## Checklist

- [ ] `pnpm lint && pnpm typecheck && pnpm test && pnpm build` passes locally
- [ ] New layout or validation behavior has tests in `packages/core/test`
- [ ] User-facing change has a changeset (`pnpm changeset`)
- [ ] README or docs updated if options or behavior changed
